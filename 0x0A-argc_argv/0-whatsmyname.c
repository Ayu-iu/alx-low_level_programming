#include "main.h"
#include <stdio.h>

/**
 * main - Entry point of the program
 * @argc: the num of comandline args
 * @argv: the array of comandline args
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
